#version 460
out vec4 FragColor;
  
in vec3 ourColor;
in vec2 TexCoord;

uniform sampler2D moTexID;
uniform int mode;

void main(){
	
	if(mode == 0){
		FragColor = texture(moTexID, TexCoord);
	}
	else{
		FragColor = texture(moTexID, TexCoord);
		FragColor = vec4(ourColor,1.0);
	}
}